# معلومات النماذج المستخدمة - Models Information

## 📊 النماذج المستخدمة في المشروع

### 1. **التلخيص (Summarization)**
- **النموذج**: `llama3` (افتراضي)
- **الحجم**: ~4.7 GB
- **الاستخدام**: `/api/summarize`
- **المتغير**: `SUMMARY_MODEL`
- **السبب**: يحتاج model كبير وقوي لإنتاج ملخصات دقيقة ومفصلة

### 2. **الأسئلة والإجابات (Q&A)**
- **النموذج**: `llama3` (افتراضي - ثابت في الكود حالياً)
- **الحجم**: ~4.7 GB
- **الاستخدام**: `/api/ask`
- **السبب**: استخدام نفس الـ model للتلخيص للحصول على إجابات أكثر دقة
- **ملاحظة**: في FastAPI Server (`api_server.py`)، يستخدم `llama3` افتراضياً. لتغيير النموذج، يجب تعديل الكود مباشرة.

### 3. **الـ Embeddings**
- **النموذج**: `nomic-embed-text` (افتراضي)
- **الحجم**: ~274 MB
- **الاستخدام**: لإنشاء vector embeddings للبحث
- **المتغير**: `EMBEDDING_MODEL`
- **السبب**: متخصص في إنشاء embeddings عالية الجودة

## 🔧 تغيير النماذج

يمكنك تغيير النماذج من خلال ملف `.env`:

```env
# للتلخيص
SUMMARY_MODEL=llama3

# للأسئلة (افتراضياً يستخدم نفس SUMMARY_MODEL)
QA_MODEL=llama3

# للـ Embeddings
EMBEDDING_MODEL=nomic-embed-text
```

## 📝 ملاحظات

1. **التلخيص والأسئلة** يستخدمان نفس الـ model (`llama3`) للحصول على دقة أعلى
2. يمكنك تغيير `QA_MODEL` في `.env` لاستخدام model مختلف إذا أردت
3. استخدام نفس الـ model يعطي إجابات أكثر دقة لكن قد يكون أبطأ قليلاً

## 🚀 النماذج المتاحة

للتحقق من النماذج المثبتة:
```bash
ollama list
```

لتحميل نماذج أخرى:
```bash
ollama pull llama3.1
ollama pull llama3.2
ollama pull mixtral
```

